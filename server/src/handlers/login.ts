/* import { Credentials } from "../models";
import { util } from "../lib";

async function loginHandler(email: string, password: string) {
    let errors: string[] = [];
  try{
    let flag = util.db.checkInDb(Credentials, { email: email});
    if(!flag) return null
  }
}
 */
